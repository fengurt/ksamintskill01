# /etc/systemd/system/apply-agent.timer
# systemctl enable --now apply-agent.timer
[Unit]
Description=Poll the in-region release inbox every 60 seconds

[Timer]
OnBootSec=2min
OnUnitActiveSec=60s
AccuracySec=10s
RandomizedDelaySec=10s
Persistent=false

[Install]
WantedBy=timers.target
