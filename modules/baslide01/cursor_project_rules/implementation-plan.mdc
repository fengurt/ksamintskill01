---
description: Feature work log for baslide01. Complete steps in order and mark Done.
alwaysApply: false
---

# Implementation plan

## Step 1 · Repo layout and TIANSIGHT demo
Done
Moved the original `files (10)` drop into `demos/TIANSIGHT/` with the documented `src/` and `docs/` layout, and restored `src/` inside the original folder so those script tags resolve.

## Step 2 · Install slide skills and templates
Done
Vendored guizang-ppt (Styles A/B/C) into `skills/guizang-ppt/` and `~/.cursor/skills/guizang-ppt`, added Atelier + TIANSIGHT 8-layout templates, and wrote `skills/TIANSIGHT-html-slides`.

## Step 3 · Local serve and existing decks
Done
Added `scripts/dev-up.sh` (ports 8765 / 8080 / 5173) and a gallery hub that links the TIANSIGHT demo, Zengcheng deck, and templates.

## Step 4 · GitHub
Done
Initialized git, created `fengurt/baslide01`, and pushed `main`.

## Step 5 · Home chrome and page-audit agent
Done
Injected 首页 / 审计 on every HTML response via `scripts/serve.py` + `assets/baslide-chrome.js`. Added `/audit/` live agent, `skills/page-audit`, and catalog coverage.

## Step 6 · Hideable top bar and page-type catalog
Done
Top bar is hideable (`H`, restore chip) and omitted on `?export=1` / print. Homepage and `/types/` are driven by `page-types.json` (17 types × skins) so generation reuses page jobs, not file names.

## Step 7 · Review, debug, free-port serve
Done
Fixed chrome hide (`display:flex` vs `[hidden]`, export class loop), directory slash 301, and audit asset collection. Added `scripts/check-pages.py` and restarted via `scripts/dev-up.sh`.

## Step 8 · Type thumbnails, template preview, loop prompts
Done
Each of the 17 types shows a 16:9 thumbnail and opens `/preview/` with the live template (`?export=1`). Per-type loop prompts live in `prompts/loop/` plus `skills/page-loop` so generation is constrained to brand tokens and layout checks.

## Step 9 · Premium PPT sample deck
Done
Saved the 5-page indigo/cyan 16:9 sample as `decks/premium-ppt/presentation.html` with `data-page-type` on cover / chapter / statement / kpi / verdict, and listed it in `catalog.json` and the homepage tools.

## Step 10 · Ref MD→HTML taxonomy (4×12×16)
Done
Audited all 7 files in `ref/`, locked 4 L1 shells × 12 L2 jobs × 16 L3 viz for a two-model pipeline, wrote `ref/AUDIT-md-to-html-taxonomy.md` and `skills/md-to-html-slides`.

## Step 11 · Original samples per slide type
Done
Filled every L2 job × 5 genres with verbatim `ref/` cuts (job excerpts + overflow + 16 viz + folded types in `gaps.md`). Regenerator is `skills/md-to-html-slides/scripts/extract-samples.py`. Size each template against the densest sample in `samples/job/<id>.md`. Combined reading copy: `ref/REPORT-md-to-html-slide-types.md`.

## Step 12 · Combined sample report
Done
Assembled taxonomy + all original samples into `ref/REPORT-md-to-html-slide-types.md` (one file, TOC, 12 jobs × 16 viz). Regenerated by the same extract-samples.py run that writes `samples/`.

## Step 13 · Collapse table types; FT viz families
Done
Dropped the 6 L3 table ids. Tables are a mark on L2 jobs with row budgets. Grouped 16 viz recipes under FT Visual Vocabulary questions. Regenerated `ref/REPORT-md-to-html-slide-types.md`.

## Step 14 · TIANSIGHT v2 templates + 06/07/08 decks
Done
Ingested `ref/TIANSIGHTtemplatespreview (1)` as `templates/TIANSIGHT/TIANSIGHT-v2.css` + 12 L2 jobs + font packs + print/PDF. Built three complete decks from 06/07/08 markdown via `scripts/build-TIANSIGHT-deck.py` (every MD table emitted; 2880×1620).

## Step 16 · Fit 2880×1620 canvas to the browser viewport
Done
Fixed present-mode layout: `#deck` now uses origin 0 0 plus translate+scale so the 2880×1620 box no longer overflows `place-items:center` into the bottom-right. Chrome offsets `#sd-stage` / `#stage` by `--baslide-chrome-h`.

## Step 18 · Bottom explain drawer + layout retune
Done
Explain panel is a bottom drawer, closed by default (tab + E). Reclaimed the old 21:9 top/footer bands so content is taller; retuned type, table, KPI, cover, and divider measure.

## Step 19 · CONFIDENCE off the canvas
Done
Removed the readme right-column A/B/C cards. Confidence is the fourth `#sd-explain` card on every data job; rebuilt 06/07/08 decks.

## Step 20 · Merge split pages and pick real viz
Done
Builder now keeps prose+table on one page, packs statements, and uses 12-row roster budget. Charts pick L3 recipes from table shape (pareto / hist-cdf / slope / heatmap / waterfall) instead of drawing every series as a bar.

## Step 21 · Chart beauty, precision, and labels
Done
Rebuilt SVG recipes: bronze/seal (no rainbow), y-grid + units, inside/above labels, hatch for small-n, sequential heatmap, dual-axis CDF. Wired unit/caption/highlight through emit; 全市西式 hist plots 门店数 (960家) and seals 55–60.

## Step 22 · Type as canvas ratio; dossier + roadmap viz
Done
Locked TIANSIGHT type to shares of 1620 (body 2.6%, title 4.4%, chrome 1.6%). Rebuilt B1.0 dossier and M1.0 roadmap with the same figure recipes and the new measure.

## Step 23 · Poster type scale + dossier/roadmap infographic
Done
Raised type to canvas-height shares (body 3.2%, title 5.2%, KPI 9.2%, cover 12%). Classified brand boards as bubble/hist and roadmap stages as timeline; stripped cite tags; rebuilt 06/07/08.

## Step 24 · Local present, cite, 烤炉, PDF, coverage
Done
Present-mode fills the local window; cite markup is stripped and asserted. 烤炉/汉堡 are sister stores, same owner. Cover lines are verdicts, not syllabi. Leftover MD prose after tables is now emitted. PDF export writes `export/pdf/` and checks 16:9 page count.

## Step 25 · Complete canvas copy; smaller type; no orphans
Done
Builder paginates statements at sentence/clause and never writes `…` on the field. Type stair is 2.7% body / 5.0% quote. `.sd-quote` / `.sd-lede` are official; 结论 tables classify as roster.

## Step 26 · ASCII walls become infographics
Done
Fenced MD diagrams are parsed as figures: weight-shift, slots, price ladder, number axis, stack. Statement pages may not carry `█` / box drawing. Rebuilt 06/07/08.

## Step 27 · Plot-axis tables become figures
Done
客单×评分 tables now emit as bubble/slope/quadrant (including 2-row 楼层对照). One unit can have multiple figures. Titles drop status emoji; overflow uses · 2. Rebuilt 06/07/08: briefing 52 charts, dossier 16, roadmap 12.

## Step 28 · Canvas typos and ugly pages
Done
Fixed split quotes after `。"`, the `「的烤炉」` rewrite, status emoji on the field, and word-joiners inside table cells. Product / 话术 tables stay roster. Rebuilt 06/07/08.

## Step 29 · Number decks and import HTML
Done
Added `decks.json` D01–D06, `/decks/` index, and D03 hub. Copied the 32-page stone HTML to `decks/stone-briefing/html-v1.html` (D03.2) and registered 清水亭 as D06 from `ref/htmls/` (it was not a deck before).

## Step 30 · Figure inventory pages + ≥20 brand map
Done
Every chart / infographic now emits a following roster of the full source rows. Briefing/dossier complete the Beijing ≥20-store brand map from 08 §2.1 (26 brands), not the 8-brand 45-yuan cut.

## Step 31 · Stone pack DB + page formulas
Done
Imported `beijing-chaoyang-heshenghui-burger-2026-06` plus SKU catalog into `decks/stone-briefing/data/pack.sqlite` (F01–F16). Linked D03.1/D03.2/D04/D05 pages; 魏斯理 gap cached from weijia1999.com. Hub is D03.3 `/decks/stone-briefing/data.html`.

## Step 32 · Complete canvas glyphs
Done
Banned `…` on the field, glued last-line CJK so a second line cannot carry 1–2 orphans, and stopped overflow clipping on rail/chip/table side lists. Checker: `scripts/check-canvas-copy.py`.

## Step 33 · 275 价格带全档 + PDF
Done
hist-cdf bin cap raised to 20 so 合生汇 15 档客单（含 100–150 / 150+）stay on the figure. Re-exported stone-briefing PDF to match the live slide count.

## Step 34 · Figure inventory + named-series completeness
Done
Audited D03.1 / D03.3 / D04 / D05: every chart has a following 清单. D05 图6/图7 pareto plotted 12 of 18/21; named-series cap is now 24 so 轻遇三明治 and 好伦哥 stay on the figure. Checker: `scripts/check-figure-inventory.py`.

## Step 35 · Page–formula verify + explain in preview
Done
Title-first formula pick so 275 charts map to F04; D03.2 titles parse. Explain drawer stays available in `/preview/` (`chrome=0` no longer equals `export=1`). Opening 解释 injects the Fxx card from `page-links.json`. Checker: `scripts/check-page-links.py`.

## Step 36 · D01 PDF skip-by-default + print freeze
Done
`check-pdf-export.py` skips 增城 unless `--all` or `zengcheng-taizikeng`. Print mode sets low-power, skips Motion/ASCII rAF so headless virtual-time can finish.

## Step 37 · Bubble readability, brand-grain 清单, un-number the pack
Done
Sqrt-area bubbles with median crosshairs, large-first paint, cluster-thinned labels. Band-chart 清单 expands to named brands from 08 §2.1 + 8–14. D03.3 is no longer a deck id; each stone deck has its own `data.html`. D03.2 exported as `export/pdf/stone-html-v1.pdf`. D03.1 424 / D04 310 / D05 206.

## Step 38 · Lock bubble and 清单 recipes in the templates
Done
`jobs/chart.html` now ships the √-area bubble; `jobs/roster.html` ships brand-grain 清单. Loop prompts, page-types checks, TIANSIGHT skill (8 rules), and md-to-html taxonomy/pipeline match the builder. CSS v2.4 documents the figure recipe on `.sd-content svg`.

## Step 39 · Smaller type, one-line header, compact lists, statement well
Done
Type stair down to 2.1% em. Header is logo + chip on one row. Bar values stay in-mark or sit outside in ink. Roster/statement leftover pages are absorbed. Transparent 侍天 logo at `templates/TIANSIGHT/logo/侍天.png` and the source logo folder. D03.1 370 / D04 280 / D05 161.

## Step 40 · Mid type + pack-by-content + fewer empty pages
Done
Default type is mid (2.45% em), not the v2.5 floor and not v2.3 2.7%. Each slide sets `data-pack=air|mid|tight` from how much copy it carries. Statements pack to ~280 chars; short lead-ins ride on the following table; 3–6 list items stay on one page; 2-bar leftovers become KPI/roster. Short qualitative frames (五个评估维度 / 三层筛选) render as stacked cards. Figure labels scale up when n is small. D03.1 325 / D04 251 / D05 148.

## Step 41 · Statement poster field
Done
Statement pages no longer glue a quote to a 序/01/02 table. The claim occupies a paper well; 1 依据 is a lede well; 2–4 依据 are peer cards; 5+ stay a full-height table. Air statement quote is 4.8% of canvas height.

## Step 42 · Bin 清单 carries share, cumulative, named samples
Done
Store-count hist 清单 no longer reprints two columns. Each bin page adds 占比 / 累计, a one-line lede, and same-unit named stores when the section has them (2.4 成功样本 + 酒饮, 3.2 代表店). Brand-grain 清单 unchanged. D03.1 324 / D04 251 / D05 148.
