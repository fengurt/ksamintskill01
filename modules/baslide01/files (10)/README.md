# Original drop

This folder is the unmodified TIANSIGHT prototype as received.

`index.html` loads scripts from `src/`. Those files now live in `src/` so this folder works when served.

The maintained copy is [`../demos/TIANSIGHT/`](../demos/TIANSIGHT/).
